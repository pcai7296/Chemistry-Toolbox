{
  "name": "chemistry-toolbox",
  "version": "1.4.2",
  "description": "Chemistry Toolbox for Xiaomi Vela wearable devices.",
  "engines": {
    "node": ">=8.10"
  },
  "scripts": {
    "prestart": "node scripts/sync_version.js",
    "start": "aiot start --watch --open-nuttx",
    "prebuild": "node scripts/sync_version.js",
    "build": "aiot build",
    "prerelease": "node scripts/sync_version.js",
    "release": "aiot release --enable-jsc",
    "check:layout": "node scripts/check_layout.mjs",
    "check:search": "node scripts/check_search.mjs",
    "check:format": "node scripts/check_format.mjs",
    "check": "npm run check:layout && npm run check:search && npm run check:format",
    "lint": "eslint --fix --ext .js \"src/**/*.js\" --no-error-on-unmatched-pattern"
  },
  "lint-staged": {
    "*.js": [
      "prettier --write",
      "eslint --fix",
      "git add"
    ],
    "*.ux": [
      "prettier --write",
      "git add"
    ],
    "*.{less,css}": [
      "prettier --write",
      "stylelint --fix --custom-syntax postcss-less",
      "git add"
    ]
  },
  "devDependencies": {
    "@aiot-toolkit/jsc": "^1.0.3",
    "aiot-toolkit": "^2.0.5"
  }
}
